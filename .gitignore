# Personal memory is local by default — remove these lines to publish it too.
memory/log.md
memory/memory.md
memory/.reorg-count

# OS / editor cruft
.DS_Store
*.swp
*~

# Temp files used by hooks
/tmp/mai-kiro-*
